Blocks:

 - Hero Top: gallery images from bg slider
 - Hero Bottom: preview mode to allow to rows rather than stacking
 - Hover blocks: preview mode to allow to rows rather than stacking
 - Hover blocks: background images
 - Jobs block:  with options
 - Tidy up code & markup; better previews, naming conventions etc

Site:
 - Pages & content
 - Working blog
 - Better page template system; one with single header/footer would be good