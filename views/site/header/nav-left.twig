<div class="rmcc-navbar-left">
  {% if theme.logo %}
    <div class="header-logo">
      {% include 'logo.twig' %}
    </div>
  {% else %}
    {{ site.title|e('esc_html') }}
  {% endif %}
  {# </a> #}
</div>