{% extends 'single.twig' %}

{% block main %}
  <article id="{{ post.id|e('esc_html') }}" class="{{ post.class|e('esc_html') }}" data-template="custom.twig">
    {% block single_content %}
      {{ parent() }}
    {% endblock %}
  </article>
{% endblock %}